{
  "name": "@skillsbay/subgraph",
  "private": true,
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy skillsbay-base-sepolia --version-label v0.1.0",
    "codegen:mainnet": "graph codegen subgraph.mainnet.yaml",
    "build:mainnet": "graph build subgraph.mainnet.yaml",
    "deploy:mainnet": "graph deploy skillsbay-base-mainnet subgraph.mainnet.yaml --version-label v0.1.0"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.98.1",
    "@graphprotocol/graph-ts": "^0.38.2"
  }
}
